// Commit 1
/* Focusing Now on the internal "command" */
for (/* Same as Before */) {
  if(/* where we're doing our calculations */) {
    /* What to do if the calculation ends up being true*/
  } else if (/* If something else is true instead */) {
    /* What to do if the second option is true */
  } else if (/* If something else instead is true */) {
    /* What to do if that third thing is true */
  } else{
    /* What to do if none of that is true */
  }
}